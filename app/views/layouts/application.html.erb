<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns:fb="https://www.facebook.com/2008/fbml">
  <head>
    <title>DCCC - Event Availability</title>
    <meta name="description" content="DCCC game availability" />
    <meta name="keywords" content="DCCC, Ruby, Rails" />
    <meta name="author" content="Koteshwar" />
    <%=javascript_include_tag :defaults %>
    <%=javascript_include_tag 'jquery.js', 'jquery-ui.js', 'fullcalendar.js', 'gcal.js', 'application.js' %>
    <%=stylesheet_link_tag "style.css", 'fullcalendar.css', 'jquery-ui.css', 'dialog.css' %>
  </head>
  <body class="subpage">
    <div class="wrapper">
      <div class="subPageContent">
        <% if flash[:notice] -%>
        <div id = 'flash_notice'>
          <%= flash[:notice] %>
        </div>
        <% end -%>
        <% if flash[:error] -%>
        <div id = 'flash_error'>
          <%= flash[:error] %>
        </div>
        <%end -%>
        <span>
        </span>
        <div class="aplicationContent">
          <div class="appRightContent">
            <%= yield %>
          </div>
        </div>
      </div>
    </div>
	<div id="calendar">
  <h2 id="month">
    <%= link_to "<", :month => (@date.beginning_of_month-1).strftime("%Y-%m") %>
    <%=h @date.strftime("%B %Y") %>
    <%= link_to ">", :month => (@date.end_of_month+1).strftime("%Y-%m") %>
  </h2>
  <% calendar_for @events, :year => @date.year, :month => @date.month do |calendar| %>
    <%= calendar.head('Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday') %>
    <% calendar.day(:day_method => :event_date) do |date, events| %>
      <%= date.day %>
      <ul>
        <% for event in events %>
          <li><%= link_to h(event.name), event %></li>
        <% end %>
      </ul>
    <% end %>
  <% end %>
</div>
    <script type="text/javascript">
      var gaJsHost = (("https:" == document.location.protocol) ? "https://ssl." : "http://www.");
      document.write(unescape("%3Cscript src='" + gaJsHost + "google-analytics.com/ga.js' type='text/javascript'%3E%3C/script%3E"));
    </script>
    <script type="text/javascript">
      try {
        var pageTracker = _gat._getTracker("UA-1010763-3");
        pageTracker._trackPageview();
        } catch(err) {}</script>
  </body>
</html>
